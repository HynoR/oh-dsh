# Copy to docker/.env (Compose loads it automatically).
# Value must equal the browser Host Nginx forwards (no :443 / :80).
# Direct localhost use can omit this file; the compose default is localhost:3080.
OH_DSH_TRUSTED_HOSTS=dsh.example.com
